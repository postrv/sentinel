# =============================================================================
# Sentinel Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your API keys

# -----------------------------------------------------------------------------
# Cloudflare Worker Secrets (set via `wrangler secret put <NAME>`)
# -----------------------------------------------------------------------------

# VirusTotal API key (free tier: 500 lookups/day)
# Get one at: https://www.virustotal.com/gui/join-us
VT_API_KEY=your_virustotal_api_key

# AbuseIPDB API key (free tier: 1000 checks/day)
# Get one at: https://www.abuseipdb.com/account/api
ABUSEIPDB_KEY=your_abuseipdb_api_key

# Shodan API key (free tier: 100 queries/month)
# Get one at: https://account.shodan.io/
SHODAN_KEY=your_shodan_api_key

# GreyNoise Community API key (free tier: 15 queries/day)
# Get one at: https://www.greynoise.io/viz/signup
GREYNOISE_KEY=your_greynoise_api_key

# URLScan.io API key (free tier: 100 scans/day)
# Get one at: https://urlscan.io/user/signup
URLSCAN_KEY=your_urlscan_api_key

# Anthropic Claude API key (for AI-powered analysis)
# Get one at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key

# -----------------------------------------------------------------------------
# Sentinel API Authentication
# -----------------------------------------------------------------------------

# Simple API secret key for authenticating requests to Sentinel
# Generate with: openssl rand -hex 32
API_SECRET_KEY=your_sentinel_api_secret_key

# -----------------------------------------------------------------------------
# Frontend Configuration (for Dashboard)
# -----------------------------------------------------------------------------

# Sentinel API URL (your Worker URL)
VITE_API_URL=https://sentinel.your-domain.workers.dev

# API key for frontend (should match API_SECRET_KEY or a generated key)
VITE_API_KEY=your_sentinel_api_key

# -----------------------------------------------------------------------------
# MCP Server Configuration (for Claude Desktop integration)
# -----------------------------------------------------------------------------

# These are set in Claude Desktop's config, not in .env
# See server.js header for configuration example
